# 🚀 دليل النشر السريع — H-HR (خطوة بخطوة)

اتبع الخطوات دي بالترتيب وهيبقى عندك موقع رسمي شغال يقدر أي حد يدخله من أي جهاز، وأي تعديل من الأدمن يظهر فوراً للكل (لأن كله بيتخزن مركزياً في قاعدة بيانات واحدة على السحابة، مش على جهازك).

---

## الفكرة باختصار

- **Railway** هيشغّل السيرفر (server.js) ويديك رابط عام (https://xxxx.up.railway.app)
- **قاعدة بيانات PostgreSQL** هتتعمل تلقائياً جوه Railway نفسه وتتربط بالسيرفر أوتوماتيك
- مفيش أي حاجة محلية — كله سحابي 100%

---

## المتطلبات قبل البدء

- [ ] حساب GitHub (مجاني) — https://github.com/signup
- [ ] حساب Railway (مجاني تسجيل، بعدين يطلب بطاقة بعد أول 5$) — https://railway.app

---

## الخطوة 1: رفع المشروع على GitHub

1. روح على https://github.com/new
2. اسم الريبو: `h-hr` (أو أي اسم)
3. خليه **Private** لو عايز تحمي بيانات شركتك
4. اضغط **Create repository**
5. من جهازك، جوه مجلد المشروع (اللي فيه الملفات المرفقة دي):

```bash
git init
git add .
git commit -m "H-HR v2 - جاهز للإنتاج"
git branch -M main
git remote add origin https://github.com/USERNAME/h-hr.git
git push -u origin main
```

> ⚠️ لو معندكش git على جهازك، حمّله من https://git-scm.com، أو استخدم زرار "Upload files" من واجهة GitHub مباشرة (اسحب كل الملفات وارفعها من المتصفح، أسهل حل لو مش عايز تتعامل مع أوامر).

---

## الخطوة 2: إنشاء المشروع على Railway

1. ادخل https://railway.app وسجّل دخول بحساب GitHub
2. اضغط **New Project**
3. اختار **Deploy from GitHub repo**
4. اختار الريبو `h-hr` اللي رفعته
5. Railway هيبدأ يبني المشروع تلقائياً (ممكن يفشل أول مرة لحد ما نضيف قاعدة البيانات — طبيعي، كمّل)

---

## الخطوة 3: إضافة قاعدة بيانات PostgreSQL

1. جوه نفس المشروع في Railway، اضغط **+ New** (أو **Create**)
2. اختار **Database** ثم **Add PostgreSQL**
3. Railway هيعمل قاعدة بيانات ويحطها جنب السيرفر بتاعك في نفس اللوحة

---

## الخطوة 4: ربط قاعدة البيانات بالسيرفر

1. اضغط على الـ Service بتاع السيرفر (مش قاعدة البيانات)
2. روح تبويب **Variables**
3. اضغط **+ New Variable** → اختار **Add Reference** → اختار قاعدة الـ Postgres اللي عملتها → هيديك `DATABASE_URL` (يتربط أوتوماتيك، مش محتاج تكتبه يدوي)
4. زوّد المتغيرات دي كمان يدوياً (زرار New Variable لكل واحد):

| المتغير | القيمة |
|---|---|
| `NODE_ENV` | `production` |
| `DEFAULT_COMPANY_NAME` | اسم شركتك |
| `DEFAULT_ADMIN_PASSWORD` | كلمة سر قوية تختارها إنت (مش Admin@123!) |
| `ALLOWED_ORIGINS` | `*` (تقدر تضيّقها بعدين على النطاق بتاعك) |

> **مهم:** الـ `.env` القديم اللي كان بيانات مشروعك فيه باسورد قاعدة بيانات Neon حقيقي — متستخدموش تاني خالص، وميستحقش يترفع على GitHub. Railway هيديك قاعدة بيانات جديدة نظيفة.

---

## الخطوة 5: تشغيل إعداد الجداول (مرة واحدة بس)

بعد ما الـ Deploy ينجح (هتشوف علامة صح خضرا)، شغّل أمر إعداد قاعدة البيانات مرة واحدة:

**الطريقة الأسهل — من Railway CLI:**

```bash
npm install -g @railway/cli
railway login
railway link      # اختار مشروعك
railway run npm run setup-db
```

**أو من تبويب "Deployments" في Railway:**
- افتح آخر Deployment → اختار **View Logs** → لو فيه زرار Shell/Terminal استخدمه واكتب:
```bash
npm run setup-db
```

هيطلعلك رسالة: `✅ تم إعداد قاعدة البيانات بنجاح!` مع بيانات الدخول الافتراضية.

---

## الخطوة 6: افتح موقعك

1. من لوحة Railway، اضغط على الـ Service بتاع السيرفر
2. تبويب **Settings** → **Networking** → اضغط **Generate Domain**
3. هيديك رابط زي: `https://h-hr-production.up.railway.app`
4. افتحه — هتلاقي شاشة تسجيل الدخول بالعربي 🎉

**بيانات الدخول:**
```
اسم المستخدم: admin
كلمة المرور: (اللي حطيتها في DEFAULT_ADMIN_PASSWORD فوق)
```

> غيّرها فوراً من داخل النظام (زرار "تغيير كلمة المرور" في القائمة الجانبية).

---

## ✅ تم! هل ده موقع رسمي فعلاً؟

- ✅ أي حد معاه الرابط يقدر يفتحه من أي جهاز/موبايل/لابتوب
- ✅ أي تعديل يعمله الأدمن (إضافة موظف، تسجيل حضور...) بيتخزن فوراً في قاعدة بيانات Railway المركزية، مش على جهاز حد
- ✅ الموقع شغال حتى لو قفلت جهازك
- ✅ HTTPS مفعّل تلقائياً من Railway (قفل أمان أخضر)

---

## بعد النشر مباشرة — افعل دول

- [ ] غيّر كلمة مرور الأدمن الافتراضية
- [ ] لو عندك نطاق مخصص (my-company.com)، من **Settings → Networking → Custom Domain** في Railway
- [ ] ضيّق `ALLOWED_ORIGINS` لنطاقك بدل `*` بعد ما تتأكد كل حاجة شغالة
- [ ] راجع صفحة "الإعدادات" جوه النظام واضبط بيانات الشركة والفروع

---

## التكلفة

Railway بيديك **5$ رصيد مجاني شهرياً**. لنظام صغير-متوسط (لحد ~50-100 موظف نشط)، ده غالباً هيكفي أو هتدفع كام دولار زيادة بسيطة شهرياً حسب الاستخدام.

---

## لو حصلت مشكلة

- **الموقع مش راضي يفتح / 502 Error** → روح **Deployments → View Logs** وشوف الخطأ
- **"قاعدة البيانات غير متصلة"** → تأكد إن متغير `DATABASE_URL` مربوط صح (خطوة 4) وإنك شغّلت `npm run setup-db` (خطوة 5)
- **نسيت كلمة مرور الأدمن** → شغّل `railway run npm run setup-db` تاني (مش هيمسح بياناتك، بس ممكن تحتاج تعمل UPDATE يدوي لو الحساب موجود بالفعل — قولّي لو حصل وهساعدك)
- لأي مشكلة تانية، راجع `TROUBLESHOOTING.md`

---

**كده المشروع كامل وجاهز فعلياً للانطلاق 🚀**
